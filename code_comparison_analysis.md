# 巡线小车代码对比分析：为什么学长的代码能成功？

## 📌 概述

本文档详细对比分析了两份 Arduino 巡线小车代码：

| 项目 | 文件路径 | 状态 |
|------|----------|------|
| **你的代码** | `exp7_redline/exp7_redline.ino` | ❌ 无法顺利巡线 |
| **学长代码** | `succession/succession.ino` | ✅ 成功通过赛道 |

两份代码使用相同的硬件平台（8路红外传感器 + 双编码器电机），但**控制策略存在根本性差异**。以下逐项分析关键区别。

---

## 🔴 一、传感器数据处理方式 — 最核心的差异

### 你的代码：单传感器逐条判断

```cpp
if (digitalRead(L5) == HIGH || digitalRead(L5) == HIGH) // 注意：这里还有bug，两个条件都是L5
if (digitalRead(L1) == HIGH) // 低右转
if (digitalRead(L6) == HIGH) // 低左转
if (digitalRead(L2) == HIGH) // 中右转
// ...每条if独立判断
```

**问题：**
- 每次只根据**某一个传感器**做决策，没有综合全部8个传感器的信息
- 当多个传感器同时检测到黑线时（弯道常见情况），只能执行最先匹配的那个 if 分支
- 传感器数据没有历史记录，容易受瞬时噪声干扰产生误判

### 学长代码：加权位置误差计算

```cpp
volatile float err[] = {-2, -1, -0.5, -0.1, 0.1, 0.5, 1, 2};
float controlInfrare() {
    sum = 0;
    for (int i = 0; i < 8; i++) {
        for (int j = 0; j < timeline; j++) {
            temp += (float)infrare[i][j];
        }
        sum += temp * err[i];  // 加权求和
        temp = 0;
    }
    return sum;
}
```

**优势：**
- 每个传感器有一个**位置权重**（err[]），从最左(-2)到最右(2)
- 综合所有8个传感器数据计算**连续的偏差值**，而不是离散的"左转/右转"判断
- 偏差值是连续量，可以反映"偏了多少"，而不仅仅是"偏了没有"
- 维护了 `timeline=2` 的历史数据缓冲，对传感器读数做**滑动平均滤波**

> 🎯 **这是成功的关键**：连续的位置偏差 → 连续的转向修正量 → 平滑的差速控制

---

## 🔴 二、转向控制策略 — 从开环到闭环

### 你的代码：开环比例控制

```cpp
if (digitalRead(L1) == HIGH) { // 低右转
    target1 = V * 0.9;
    target2 = V * 0.5;
}
```

**问题：**
- 每个弯道场景预设了固定的左右轮速度比（如 0.9:0.5）
- 这是**开环控制**：不看实际效果，不管偏差大小，始终用同一个比例
- 无法适应不同曲率的弯道（椭圆弯 vs 直角弯需要不同的转向力度）
- 速度比是拍脑袋定的，没有反馈校正

### 学长代码：级联 PID 闭环控制

```
红外偏差 → 转向PID(外层) → delta_speed → 左右轮目标速度 → 速度PID(内层) → PWM输出
```

外层转向 PID：
```cpp
float pidToSlow(float error) {
    float u = 0, kp = 8.0, kd = 0.4;
    FeI = error;
    u = kp * FeI + kd * (FeI - FeII);  // PD控制
    FeII = FeI;
    if (u > delta_speed_max) u = delta_speed_max;
    if (u < -delta_speed_max) u = -delta_speed_max;
    return u;
}
```

内层速度 PID：
```cpp
int pidcontrol_L(float target, float current) {
    LeI = target - current;
    float kp = 30, TI = 25, TD = 15, T = PERIOD;
    // ... 标准位置式PID
}
```

**优势：**
- **级联控制架构**：外层根据位置偏差计算需要的差速量，内层精确跟踪目标速度
- 偏差大时自动加大转向力度，偏差小时自动减小 — **自适应**
- PD 控制器中的微分项(D)提供了**预测性**：偏差变化快时提前反应，防止过冲
- delta_speed 有上下限保护，防止急转弯翻车

---

## 🔴 三、PID 参数差异

| 参数 | 你的代码 | 学长代码 | 影响 |
|------|----------|----------|------|
| 速度环 Kp | 7.0 | 30.0 | 学长响应速度快 4 倍+ |
| 速度环 Ti | 50.0 | 25.0 | 学长积分作用更强（消除稳态误差） |
| 速度环 Td | 10.0 | 15.0 | 学长微分作用更强（抑制超调） |
| 转向环 Kp | ❌ 无 | 8.0 | 你缺少转向 PID |
| 转向环 Kd | ❌ 无 | 0.4 | 你缺少转向 PID |
| 基础速度 | 3.0 | 6.0 | 学长基础速度快一倍 |

**分析：**
- 你的速度环 Kp=7 太小，电机响应迟钝，来不及在弯道快速调整
- 学长 Kp=30 保证了对目标速度的快速跟踪
- 学长的 Ti 更小（积分时间常数更小 → 积分作用更强），能消除稳态速度误差

---

## 🔴 四、电机方向控制

### 你的代码：方向控制不完整

```cpp
// setup() 中只设置了一次
digitalWrite(DIR1, HIGH);
// DIR2 从未显式设置！
// control() 中从未根据 PID 输出改变方向
analogWrite(PWM1, abs(output1));
analogWrite(PWM2, abs(output2));
```

**问题：**
- DIR2 引脚从未被显式设置为输出或给定电平，处于不确定状态
- 控制函数中始终用 `abs(output)` 输出 PWM，意味着**电机只能单向转**
- 当 PID 输出负值时（需要反转减速），你只取了绝对值给 PWM，没有改变方向

### 学长代码：完整的方向控制

```cpp
void control_L() {
    Loutput = pidcontrol_L(TARGET_L, velocity_L);
    if (Loutput > 0) {
        digitalWrite(DIRL, HIGH);
        analogWrite(PWML, Loutput);
    } else {
        digitalWrite(DIRL, LOW);
        analogWrite(PWML, abs(Loutput));
    }
}
```

**优势：**
- 根据 PID 输出的正负号动态切换电机方向
- 正转时 DIR=HIGH，反转时 DIR=LOW
- 两个电机都能**双向运行**，这是实现差速转向的基础

---

## 🔴 五、状态机设计

### 你的代码：无状态管理

`loop()` 中只做串口打印，`control()` 一直在运行。没有巡线、停止的概念区分。代码上电后立刻开始 PID 控制，不管小车是否已经放在赛道上。

### 学长代码：巡线 → 停止两阶段

```
┌──────────┐    检测全黑(终点)   ┌──────────┐
│   巡线    │ ────────────────→ │   停止    │
│ isTrace  │                    │ allFinish │
│ = true   │                    │ = true    │
└──────────┘                    └──────────┘
```

**阶段1 - 巡线（control中）：**
```cpp
if (isTrace) {
    controlDelta();   // 计算差速转向
    control_L();      // 左电机速度控制
    control_R();      // 右电机速度控制
}
```

**阶段2 - 停止（loop中）：**
```cpp
if (isFinish && !allFinish) {
    TARGET_L = 0;
    TARGET_R = 0;
    control_L();
    control_R();
    allFinish = true;
}
```

**优势：**
- 通过检测 8 个传感器全黑来识别终点线（`isAllBlack()`）
- 到达终点后自动停车，目标速度置零
- `control()` 受 `isTrace` 控制，不会在非巡线状态下误运行

---

## 🔴 六、异常情况处理

### 你的代码：无异常处理

没有任何针对"丢失黑线"或"到达终点"的处理逻辑。

### 学长代码：完善的边界情况处理

```cpp
// 全黑 → 终点线检测
bool isAllBlack() {
    for (int i = 0; i < 8; i++)
        if (infrare[i][0] == 0) return false;
    return true;
}

// 全白 → 丢失循线，保持上一时刻的转向量并逐渐增强
bool isAllWhite() {
    for (int i = 0; i < 8; i++)
        if (infrare[i][0] == 1) return false;
    return true;
}

// 全白时的处理
if (delta_speed > 0 && delta_speed < delta_speed_max)
    delta_speed += 0.28 * BASESPEED;  // 保持转向趋势并加强
else if (delta_speed < 0 && delta_speed > -delta_speed_max)
    delta_speed -= 0.28 * BASESPEED;
```

**分析：**
- 全白时说明传感器全部偏离了黑线，此时**保持上一时刻的转向方向并逐步加强**，让小车"猜"着往回找线
- 这是一种简单但有效的"丢失恢复"策略

---

## 🔴 七、代码 BUG

你的代码第 45 行存在一个明显 bug：

```cpp
if (digitalRead(L5) == HIGH || digitalRead(L5) == HIGH)
//                              ^^^^^^^^^^^^^^^^^^^^^^^^
//                              两个条件完全相同！应该是 L4 和 L5（中间两个传感器）
```

正确写法应该是：
```cpp
if (digitalRead(L4) == HIGH || digitalRead(L5) == HIGH)
```

**影响：** 这使得直行条件永远只依赖 L5 一个传感器，中间位置检测不准确，小车可能在直线段就开始抖动。

---

## 🔴 八、架构层面的差异总结

| 维度 | 你的代码 | 学长代码 |
|------|----------|----------|
| 传感器融合 | 单传感器逐条 if-else | 8 传感器加权求和 |
| 传感器滤波 | 无 | 2 帧滑动平均 |
| 转向方式 | 预设速度比（开环） | 位置偏差 PID → 差速量（闭环） |
| 控制架构 | 单层速度 PID | 级联：转向 PD + 速度 PID |
| 电机方向 | 单向（仅 abs 输出） | 双向（根据符号切换） |
| 状态管理 | 无 | 初始化→巡线→停止 |
| 终点检测 | 无 | 8 传感器全黑判断 |
| 丢失恢复 | 无 | 保持趋势 + 逐步增强 |
| PID 参数 | Kp=7（偏保守） | Kp=30（响应快） |
| 基础速度 | 3.0 | 6.0 |

---

## 🎯 核心结论

学长代码成功的最根本原因可以归结为一条：

> **将"离散的传感器触发判断"改为了"连续的位置偏差闭环控制"。**

你的代码思路是：「L2 看到了黑线 → 执行中右转」，每个传感器对应一个固定动作。这在理想条件下可能勉强工作，但真实赛道有各种曲率的弯道，光照不均，传感器有噪声，固定的速度比无法应对。

学长的代码思路是：「所有 8 个传感器告诉我偏差了 +2.3 → 转向 PID 计算出需要 delta_speed=18 → 左轮减速 18，右轮不变 → 速度 PID 精确执行」。这是一个完整的**感知→决策→执行**闭环，每个环节都有反馈校正。

---

## 📝 改进建议（按优先级排序）

如果要修改你的代码，建议按以下顺序进行：

1. **修复 L5 重复判断的 bug**（第 45 行）
2. **加入 8 传感器加权位置误差计算**，替代单传感器 if-else
3. **增加外层转向 PD/PID 控制器**，实现连续差速转向
4. **完善电机方向控制**，使电机能正反转
5. **加入传感器历史数据滤波**，减少噪声干扰
6. **提高 PID 参数**（Kp 从 7 → 20~30）
7. **增加终点检测和停止逻辑**

---

> 📅 文档生成日期：2026-06-12
> 📁 项目路径：`E:\firstcc\line_tracking_car\`
